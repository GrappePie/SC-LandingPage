export const EmailTemplate = ({name,email,telephone}) => {
    return (
        <div>
            <h1>Nombre: {name}</h1>
            <h1>Email: {email}</h1>
            <h1>Telefono: {telephone}</h1>
        </div>
    )
}